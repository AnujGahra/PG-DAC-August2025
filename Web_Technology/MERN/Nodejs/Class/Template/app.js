// A Template enigine facilitates you to uuse static template files in your applications.

// At run time, it replaces variables in a template file with actual values, and transform the template into an HTML file sent to the client.
// This approach make it easier to design an HTML pages.

